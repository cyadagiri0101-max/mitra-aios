# M11.6 Lineage Certification Document

## 1. Multi-Hop Graph Lineage Verification
The complete end-to-end evidence lineage is verified across all entities:
`RFQ / Drawing Revision` $\rightarrow$ `Geometric Feature` $\rightarrow$ `DFM Finding` $\rightarrow$ `Defect Correlation` $\rightarrow$ `EKOS Evidence` $\rightarrow$ `Reasoning Result` $\rightarrow$ `Cost Synthesis` $\rightarrow$ `Recommendation` $\rightarrow$ `Human Decision` $\rightarrow$ `Audit Record`.

## 2. Invariants
- `tenantId` and `drawingRevision` preserved on every hop.
- Immutable SHA-256 evidence hashes ensure tamper-evident reasoning integrity.
- Zero orphaned or cross-tenant lineage edges.
