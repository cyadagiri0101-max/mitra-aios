# M9.4 — Migration Specification
## Database Migration 1700000000049-M9ModelRegistryGovernance.ts
**Milestone:** `M9.4`  
**Status:** **AUTHORITATIVE & VERIFIED**

---

## 1. Migration Overview

- **Migration File:** `1700000000049-M9ModelRegistryGovernance.ts`
- **Table Created:** `g14_model_registry`
- **Indexes:**
  - `IX_g14_model_registry_tenant` on `(tenant_id)`
  - `IX_g14_model_registry_version` on `(tenant_id, model_version)`
  - `IX_g14_model_registry_capability` on `(tenant_id, capability, status)`
  - `UQ_g14_model_registry_active_champion` (Unique partial index on `(tenant_id, capability)` WHERE `status = 'ACTIVE' AND deleted_at IS NULL`)
- **Safety:** Additive, non-destructive, reversible.
