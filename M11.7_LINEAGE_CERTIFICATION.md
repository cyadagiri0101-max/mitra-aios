# M11.7 Lineage Certification Document

## 1. Multi-Hop Provenance Graph
The end-to-end lineage path is verified:
`RFQ / Drawing Revision` $\rightarrow$ `Geometric Feature` $\rightarrow$ `DFM Finding` $\rightarrow$ `Historical Defect Correlation` $\rightarrow$ `EKOS Lineage` $\rightarrow$ `Reasoning Result` $\rightarrow$ `Governed Cost Summary` $\rightarrow$ `Advisory Recommendation` $\rightarrow$ `Human Decision` $\rightarrow$ `Audit Trail`.

## 2. Invariant Verification
- Immutable SHA-256 evidence hashing prevents tampering.
- Zero dangling, orphaned, or cross-tenant edges in the relational model and EKOS graph.
