# M11.6 Data Integrity Certification Document

## 1. Relational & Schema Integrity
- Full referential integrity preserved across `geometric_features`, `dfm_findings`, `historical_defect_correlations`, `engineering_reasoning_results`, and `engineering_cost_configurations`.
- Deterministic SHA-256 evidence hashing prevents duplicate reasoning records and guarantees tamper-evident provenance.
- Optimistic locking (`version` column) prevents concurrent update corruption.
